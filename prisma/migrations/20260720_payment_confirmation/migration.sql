-- Payroll Confirmation Engine (additive)
-- Applied via prisma db push; documents intent for payment_confirmations,
-- payment_confirmation_files, app_notifications, execution_model, payroll statuses.
SELECT 1;
